export default function getListStudents() {
  const myArr = [];

  myArr.push({ id: 1, firstName: 'Guillaume', location: 'San Francisco' });
  myArr.push({ id: 2, firstName: 'James', location: 'Columbia' });
  myArr.push({ id: 5, firstName: 'Serena', location: 'San Francisco' });

  return myArr;
}
